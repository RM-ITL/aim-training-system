关于 io 硬件中的 gimbal 云台。
首先，我们要知道，前面的 detect、point、solver、target、tracker 做了那么多事情，并不是为了把结果留在电脑里自己欣赏的。它们最后总得把结果真正交给一个能动起来的东西。而在自瞄这条链路里，视觉和下位机之间最关键的桥梁之一，就是 gimbal 云台。

那么 gimbal 在这里到底是做什么的呢？
它做的事情，本质上不是“帮我们识别目标”，也不是“帮我们预测目标”，而更像一个翻译官和交通枢纽。一边，它要接住下位机发上来的状态信息，比如当前模式、四元数姿态、yaw、pitch、角速度、弹速、弹丸计数这些内容；另一边，它又要把视觉算出来的控制命令发回去，比如要不要控制、要不要开火、目标yaw/pitch、角速度、角加速度这些东西。

所以，gimbal 做的事情，本质上就是一句话：把“视觉这边算出来的结果”和“下位机那边真实的云台状态”稳定地连起来。

那么，为什么这一步很重要呢？
因为视觉处理每一帧图像的时候，不只是要知道“看到了什么”，还要知道“看见这张图的时候，云台当时到底朝哪儿”。如果这件事对不齐，前面再好的检测、解算、预测，最后也可能打歪。所以 gimbal 这里有一个特别重要的任务，就是姿态和图像时间的对齐。

gimbal 这条链路其实也很清楚：
下位机持续回传模式、姿态和云台状态  ->  gimbal 后台线程接收并解析  ->  对四元数做标定补偿并存入时间队列 -> 上层按图像时间戳来取对齐后的姿态  ->  视觉再把算出来的控制量通过 gimbal 打包发回下位机  ->  如果串口异常gimbal 再负责检测、保护和自动重连。

详细请看 io 硬件中的 gimbal 云台文件。让我们在 gimbal 下面见。：）

重点：
- 重点1：gimbal 不是检测器，也不是预测器，它是视觉和下位机之间的双向通信桥梁。
- 重点2：这里最关键的事情之一，是“图像时间”和“云台姿态时间”的对齐。
- 重点3：q(timestamp) 这种按时间戳取姿态的接口，是整条自瞄链路里非常关键的基础
能力。
- 重点4：gimbal 不只是发命令，还要持续接收模式、姿态、yaw/pitch、弹速等状态信
息。
- 重点5：串口异常检测、自动重连、发送保护这些内容，属于工程上非常重要但很容易
被忽视的部分。
- 重点6：下位机上传的原始四元数还要经过标定补偿，不能直接默认拿来就用。

--候儆预

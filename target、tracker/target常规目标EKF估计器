Auto_aim_ITL/src/core/auto_aim/target/predictor/include/target.hpp

常规目标类主要服务于普通步兵、平衡步兵和基地。它的核心思想是:把“目标旋转中心 + 旋转角速度 + 半径参数 + 几何差异参数”作为状态，用一帧一帧的装甲板观测去修正这个状态。
它的状态向量在构造函数里初始化为 11 维：x = [cx, vx, cy, vy, cz, vz, θ, ω, r, l, h]。含义可以直接理解为：
- cx, cy, cz：目标旋转中心位置
- vx, vy, vz：目标中心速度
- θ：当前参考装甲板相位角
- ω：角速度
- r：基础半径
- l：长短轴半径差
- h：高低差
初始化时，它用首帧装甲板的位置和朝向反推出旋转中心，然后把速度和几何差值先置零，构造内部 EKF。
预测阶段在 Auto_aim_ITL/src/core/auto_aim/target/predictor/src/target.cpp:80。它做的是典型“匀速平移 + 匀角速度旋转”模型：
- cx, cy, cz 按各自速度推进
- θ 按 ω 推进
- r, l, h 大体视为慢变量或常量
这里过程噪声 Q 比前哨站大很多，v1=100、v2=400，说明它默认普通目标机动性更强、运动不确定性更大。
更新阶段可分两步：
第一步是更新前的准备阶段“板号匹配”，相较于前哨站目标，它不是用完整马氏距离进行匹配，而是一个更简单的启发式匹配：
- 先用当前状态算出所有候选装甲板的预测位置 armor_xyza_list()
- 按距离排序，只看最近的最多 3 块
- 比较观测装甲板与每块候选板的两类角度误差：
  - 装甲板朝向误差 world_orientation.yaw - xyza[3]
  - 球坐标 yaw 误差 world_spherical.yaw - ypd[0]
- 误差最小的板号作为当前 id
第二步是 EKF 观测更新，它使用 4 维观测：z = [yaw, pitch, distance, angle]。也就是：
- world_spherical.yaw
- world_spherical.pitch
- world_spherical.distance
- world_orientation.yaw
观测函数 h(x) 是：
先根据当前状态算出某块装甲板的 xyz，再转成 ypd，最后加上该板的朝向角。它的几何模型关键在：
- 如果 armor_num_ == 4，则 id=1/3 这两块板使用 r+l 和 cz+h
- id=0/2 使用 r 和 cz
所以常规目标实际上在表达“交替长短轴、交替高度”的四板模型。如果是基地或平衡步兵，则 armor_num_ 不是 4，l/h 基本不参与几何变化，退化成更简单模型。它还有两个和跟踪稳定性有关的附加逻辑：
- single_plate_mode：如果很久没有切板且角速度很低，就进入单板模式
- 收敛判据很宽松：更新次数大于 3 且不发散即可
- 发散判据主要看几何参数是否物理合理：r 和 r+l 要在 (0.05, 0.5) 内

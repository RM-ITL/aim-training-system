关于目标状态估计与跟踪 target、tracker，首先，我们要知道，到了 target 和 tracker 这里，事情已经不是“这一帧有没有看见目标”这么简单了。在来到这里之前，detect 已经帮我们找到了装甲板，solverpnp 也已经把角点和姿态信息变成了 Armor_pose，也就是一个已经能放到空间里去理解的目标观测。
但是，这还不够。
因为相机给我们的东西，是一帧一帧、带噪声的、会抖的、还可能突然丢一下的观测。这一帧看见了，不代表下一帧还看得清；这一帧看到的是这一块板，下一帧也可能切到另一块板；目标自己还可能平移、旋转、跳板。如果我们每一帧都把它当成一个全新的目标，那么后面的瞄准就会很不稳定。
所以，target 和 tracker 做的事情，本质上就是一句话：
把“离散的装甲板观测”，变成“连续的、稳定的、可以预测的目标状态”。

那么这部分下面先看什么呢？
请先看 KF和EKF。这个文件先告诉我们，后面这一整套目标状态估计到底靠什么思想在工作。它最核心的事情其实很朴素，就是两步：预测 + 更新。先根据上一时刻的状态，猜一猜当前目标应该在哪里；再拿当前这一帧真正观测到的结果，去修正刚才这个“猜测”。 KF 适合线性系统，EKF 适合非线性系统，而我们的自瞄目标跟踪，显然更接近后者。所以你可以先把这个文件理解成：它在给后面的 target、predictor、motion_module 打地基。

如果我们知道了 EKF 大概在干什么，下一步请看 predictor。
predictor 讲的是整条链路到底怎么接起来：
detect 先识别装甲板，solverpnp 再把它解算成 Armor_pose，然后 tracker 接过这些观测，选择目标、判断目标类型，再去调用 predictor 做真正的状态估计和预测。

继续往下走，想看普通装甲板目标到底是怎么估计的，就要看 target常规目标EKF估计器。这个文件讲的是普通步兵和基地这一类常规目标。它不是只盯着“当前看到的是哪一块板”，而是在回答：
-   这辆车的旋转中心在哪里？
-   它现在往哪动？
-   它转得快不快？
-   这一圈装甲板的几何关系是什么样的？
所以它维护的不是一个小小的点，而是一整套目标状态。它会先根据首帧观测反推出旋转中心，再做预测；之后不是立刻就更新，而是先做板号匹配，先搞清楚“当前看到的到底是哪一块板”，然后才用 EKF 去做真正的观测更新。这样做的目的，就是把“看见某块板”慢慢变成“理解整辆车”。

那么，前面这些预测和更新，真正的是motion_module（运动模块）在底层帮我们计算。
它可以理解成这一整套目标状态估计的发动机。前面的 KF和EKF 讲的是思想，predictor 讲的是链路，target常规目标EKF估计器 讲的是普通目标怎么建模， 而 motion_module 讲的是：这些预测矩阵、观测更新、协方差推进、残差统计，到底在程序里是怎么真正落下来的。
这里面除了状态向量 x 和协方差矩阵 P，还会记录 NIS、NEES、残差这些东西，来帮助上层判断这个目标是不是还靠谱，是不是已经开始发散了。
所以如果前面三个文件是在告诉你“为什么要这样做”，那么 motion_module 就是在告诉你“这些事情到底是怎么被实现出来的”。

所以，target 和 tracker 这条链路其实很清楚：
detect 先找到装甲板 -> solverpnp 把装甲板变成空间观测 Armor_pose -> tracker 先决定这个目标该不该跟、能不能继续跟 -> predictor 负责把观测交给具体目标估计器 ->target常规目标EKF估计器 去理解普通目标的整车运动状态 -> motion_module 在底层完成预测和更新 -> 最后把更稳定的目标状态交给后面的瞄准与控制模块。

详细请看 target、tracker 下面的 KF和EKF、predictor、motion_module（运动模
块）、target常规目标EKF估计器 文件。

--候儆预

如果我们已经知道traget和tracker,我们已经做好了瞄锥装甲板的准备，下一步就是规划与射击
我们肯定会有疑问，规划是什么？射击是什么？
规划就是，在自瞄系统里，就是拿到目标的未来位置后，决定云台该怎么动、什么时候开枪，才能最稳、最准地击中目标。它不是简单地“指过去”，而是要打“提前量”。
射击就是，规划指令的最终安全执行。
在规划中，
如果说位姿解算和跟踪器的作用是回答“目标现在在哪儿、往哪儿跑”，那么规划的核心任务就是解决两个问题：
1. 预测：当子弹飞到目标跟前时，目标会运动到什么位置？
2. 控制：云台如何平滑、快速地转过去，并在最准的瞬间开火？
弹道补偿 (算未来点)->系统延迟补偿(对齐时间轴)->MPC 轨迹优化(规划最优路径)->开火判断（规划层,只有当偏航和俯仰的总误差都小于 fire_thresh_ 时，plan.fire 才为 true）
在射击中，存在两层开火门控，
暂时无法在飞书文档外展示此内容
只有两层都通过，才真正发送开火指令。
同时在其中，在理解射击逻辑时，必须区分两种 yaw_offset，这是不一样的。
暂时无法在飞书文档外展示此内容
Planner::plan(Target, bullet_speed)
    │
    ├─ 选择最近装甲板
    ├─ 计算子弹飞行时间
    ├─ target.predict(fly_time)          [打未来点]
    ├─ aim() → 当前时刻理想瞄准角
    ├─ get_trajectory() → 100步参考轨迹
    ├─ 分别求解偏航、俯仰 MPC            [TinyMPC]
    ├─ 填充 Plan {control, fire, yaw, pitch, vel, acc}
    │
    ▼
Shooter::checkfire(cmd_yaw, cmd_pitch, gs, target)
    │
    ├─ 根据距离选择容差 (first/second_tolerance_)
    ├─ 计算闭环误差 (cmd - gs)
    ├─ 归一化误差 < 1.0 ?
    │
    ▼
plan.fire && checkfire → 最终开火指令
    │
    ▼
Gimbal::send() → 云台执行 & 射击
到这里，我们对自瞄就有一个整体的了解了：）
（王苑）
